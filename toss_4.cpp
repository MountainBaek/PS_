#include <iostream>
#include <string>
using namespace std;
struct node {
	string date;
	node* next;
};
int main() {
	ios::sync_with_stdio(0), cin.tie(0);
	
	while (!cin.eof()) {
		string input;
		cin >> input;
		
	}

	return 0;
}